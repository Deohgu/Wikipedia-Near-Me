import styled from "styled-components";

export const Locations = styled.a`
  color: black;
  /* position: absolute;
  right: ${(props) => (props.lat * 100) / 414 + "%"};
  top: ${(props) => (props.lon * 100) / 736 + "%"}; */
`;
